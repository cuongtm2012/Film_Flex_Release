# Docker Environment Configuration
# Copy this file to .env.docker for Docker deployment

NODE_ENV=production
PORT=5000
DATABASE_URL=postgresql://filmflex:filmflex2024@postgres:5432/filmflex
SESSION_SECRET=filmflex_production_secret_2024_change_this_in_production
ALLOWED_ORIGINS=*
CLIENT_URL=*
DOMAIN=localhost
SERVER_IP=localhost

# For production deployment, update these:
# DOMAIN=phimgg.com
# SERVER_IP=your.server.ip
# SESSION_SECRET=your_secure_random_secret